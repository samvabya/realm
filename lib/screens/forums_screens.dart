import 'package:flutter/material.dart';

class ForumsScreens extends StatelessWidget {
  const ForumsScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Forums Screen'),
      ),
    );
  }
}